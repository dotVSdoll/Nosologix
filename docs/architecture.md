# Architecture Notes

Current MVP keeps the architecture intentionally small:

```text
Client -> FastAPI -> Services -> RAG/Agents -> Storage
```

Detailed draw.io diagrams will be added after the RAG and agent workflow stabilizes.
